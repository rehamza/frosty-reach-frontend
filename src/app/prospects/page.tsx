"use client";

import ProspectContainer from "./Container";
// if page.tsx is client then call and handle APIs in container and if we use server side call API's in page.tsx

const Prospect = () => {
  return <ProspectContainer />;
};

export default Prospect;
